@model PharmaChain.ViewModels.SupplierDashboardViewModel

@{
    ViewData["Title"] = "Supplier Dashboard";
}

<div class="row">
    <div class="col-12">
        <h1 class="mb-4">Supplier Dashboard</h1>
    </div>
</div>

<div class="row mb-4">
    <div class="col-md-3">
        <div class="card bg-primary text-white">
            <div class="card-body">
                <h5 class="card-title">Total Inventory</h5>
                <h2>@Model.TotalInventory</h2>
            </div>
        </div>
    </div>
    <div class="col-md-3">
        <div class="card bg-info text-white">
            <div class="card-body">
                <h5 class="card-title">Total Orders</h5>
                <h2>@Model.TotalOrders</h2>
            </div>
        </div>
    </div>
    <div class="col-md-3">
        <div class="card bg-warning text-white">
            <div class="card-body">
                <h5 class="card-title">Pending Orders</h5>
                <h2>@Model.PendingOrders</h2>
            </div>
        </div>
    </div>
    <div class="col-md-3">
        <div class="card @(Model.LowStockItems > 0 ? "bg-danger" : "bg-success") text-white">
            <div class="card-body">
                <h5 class="card-title">
                    <i class="fas fa-exclamation-triangle"></i> Low Stock Items
                </h5>
                <h2>@Model.LowStockItems</h2>
                @if (Model.LowStockItems > 0)
                {
                    <small>Items with ≤ @PharmaChain.Models.Inventory.LOW_STOCK_THRESHOLD units</small>
                }
            </div>
        </div>
    </div>
</div>

<!-- Low Stock Alert -->
@if (Model.LowStockItems > 0)
{
    <div class="row mb-4">
        <div class="col-12">
            <div class="alert alert-danger alert-dismissible fade show" role="alert">
                <h4 class="alert-heading">
                    <i class="fas fa-exclamation-triangle"></i> Low Stock Alert!
                </h4>
                <p class="mb-0">
                    You have <strong>@Model.LowStockItems</strong> inventory item(s) with low stock (≤ @PharmaChain.Models.Inventory.LOW_STOCK_THRESHOLD units).
                    Consider buying more stock from manufacturers to avoid stockouts.
                </p>
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
            </div>
        </div>
    </div>
}

<!-- Low Stock Items Table -->
@if (Model.LowStockInventory.Any())
{
    <div class="row mb-4">
        <div class="col-12">
            <div class="card border-danger">
                <div class="card-header bg-danger text-white">
                    <h5 class="mb-0">
                        <i class="fas fa-exclamation-triangle"></i> Low Stock Inventory
                    </h5>
                </div>
                <div class="card-body">
                    <div class="table-responsive">
                        <table class="table table-striped">
                            <thead>
                                <tr>
                                    <th>Medicine Name</th>
                                    <th>Current Stock</th>
                                    <th>Price</th>
                                    <th>Status</th>
                                    <th>Action</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model.LowStockInventory)
                                {
                                    <tr class="@(item.Quantity <= 2 ? "table-danger" : "table-warning")">
                                        <td>@item.Medicine?.Name</td>
                                        <td>
                                            <span class="badge bg-@(item.Quantity <= 2 ? "danger" : "warning")">
                                                @item.Quantity units
                                            </span>
                                        </td>
                                        <td>@item.Medicine?.Price.ToString("C")</td>
                                        <td>
                                            @if (item.Quantity <= 2)
                                            {
                                                <span class="badge bg-danger">Critical</span>
                                            }
                                            else
                                            {
                                                <span class="badge bg-warning">Low</span>
                                            }
                                        </td>
                                        <td>
                                            <a asp-action="BuyFromManufacturer" class="btn btn-primary btn-sm">
                                                <i class="fas fa-shopping-cart"></i> Buy More
                                            </a>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
}


<div class="row">
    <div class="col-12">
        <div class="card">
            <div class="card-header">
                <h5>Recent Orders</h5>
            </div>
            <div class="card-body">
                @if (Model.RecentOrders.Any())
                {
                    <div class="table-responsive">
                        <table class="table table-striped">
                            <thead>
                                <tr>
                                    <th>Order ID</th>
                                    <th>Medicine</th>
                                    <th>Quantity</th>
                                    <th>Total Amount</th>
                                    <th>Status</th>
                                    <th>Order Date</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var order in Model.RecentOrders)
                                {
                                    <tr>
                                        <td>@order.OrderID</td>
                                        <td>@order.Medicine?.Name</td>
                                        <td>@order.Quantity</td>
                                        <td>@order.TotalAmount.ToString("C")</td>
                                        <td>
                                            <span class="badge bg-@(order.Status switch {
                                                OrderStatus.Pending => "warning",
                                                OrderStatus.Approved => "info",
                                                OrderStatus.Delivered => "success",
                                                OrderStatus.Rejected => "danger",
                                                _ => "secondary"
                                            })">
                                                @order.Status
                                            </span>
                                        </td>
                                        <td>@order.OrderDate.ToString("MM/dd/yyyy")</td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                }
                else
                {
                    <p class="text-muted">No recent orders found.</p>
                }
            </div>
        </div>
    </div>
</div>
